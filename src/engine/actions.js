/** actions.js — RETIRED (V2 Phase 3). Split into engine/passingSystem.js + engine/shootingSystem.js. */
export class ActionSystem {
  constructor() { throw new Error('ActionSystem is retired — use PassingSystem / ShootingSystem'); }
}
