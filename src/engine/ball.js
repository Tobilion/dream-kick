/** ball.js — RETIRED (V2 Phase 3). Use BallPhysics from engine/ballPhysics.js. */
export class Ball {
  constructor() { throw new Error('Ball is retired — use BallPhysics from engine/ballPhysics.js'); }
}
